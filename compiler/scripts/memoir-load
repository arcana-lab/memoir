#!/bin/bash -e
GIT_ROOT=`git rev-parse --show-toplevel` ;
LIB_DIR=${GIT_ROOT}/install/lib ;

source ${GIT_ROOT}/enable ;

LOAD_PASSES=$(find ${LIB_DIR} -name "*.so" -printf ' -load %p ')

echo "${LOAD_PASSES}"

noelle-load "${LOAD_PASSES}" "$@"
